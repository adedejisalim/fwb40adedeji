extends layout

block content
  h1= title
  - let image_names = ["a1.jpg", "a2.jpg", "a3.jpg", "a4.png", "a5.png"]
  - for (var i=0; i<3;i++) {
      - let idx = Math.floor(Math.random()*5);
      img(src=`/images/${image_names[idx]}`, alt="", height="300", width="300")
  - }